# 🔧 КРИТИЧЕСКАЯ ПРОБЛЕМА: База данных Supabase недоступна

## ❌ Проблема
Все попытки подключения к Supabase заканчиваются ошибкой `ECONNRESET` (соединение сбрасывается).

## 🔍 Найденные проблемы

1. **Pooler недоступен** - порт 6543 не отвечает или блокируется
2. **Все варианты подключения не работают:**
   - Транзакционный pooler (порт 6543)
   - Прямое подключение (порт 5432) 
   - Session pooler
   - Pooler с SSL

## ✅ РЕШЕНИЯ (попробуйте в этом порядке)

### Решение 1: Используйте прямое подключение

Откройте `.env` и измените `DATABASE_URL`:

**БЫЛО (pooler - не работает):**
```
DATABASE_URL=postgresql://postgres.chkziqbxvdzwhlucfrza:ПАРОЛЬ@aws-1-eu-west-2.pooler.supabase.com:6543/postgres
```

**СТАНЕТ (прямое подключение):**
```
DATABASE_URL=postgresql://postgres.chkziqbxvdzwhlucfrza:ПАРОЛЬ@aws-1-eu-west-2.supabase.co:5432/postgres
```

**После изменения:**
1. Сохраните файл
2. Перезапустите сервер: `npm run dev`

### Решение 2: Проверьте блокировку IP в Supabase

1. Зайдите в [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите проект `chkziqbxvdzwhlucfrza`
3. Перейдите в **Settings** → **Database**
4. Найдите раздел **Connection Pooling** или **Network Restrictions**
5. Проверьте **Allowed IP addresses**
6. Если там есть ограничения - **добавьте ваш текущий IP** или **удалите ограничения**

### Решение 3: Проверьте статус проекта Supabase

1. Зайдите в [Supabase Dashboard](https://supabase.com/dashboard)
2. Проверьте статус проекта:
   - ❌ Если проект **Paused** (приостановлен) - восстановите его
   - ❌ Если проект **Deleted** - нужно создать новый
   - ✅ Если проект **Active** - проблема в настройках

### Решение 4: Сбросьте пароль базы данных

1. В Supabase Dashboard → **Settings** → **Database**
2. Найдите **Database Password**
3. Нажмите **Reset Database Password**
4. Скопируйте новый пароль
5. Обновите `DATABASE_URL` в `.env` с новым паролем

### Решение 5: Проверьте Connection Pooling настройки

1. В Supabase Dashboard → **Settings** → **Database** → **Connection Pooling**
2. Убедитесь что **Connection Pooling** включен
3. Попробуйте изменить режим:
   - **Transaction mode** (рекомендуется)
   - **Session mode**

## 🧪 Проверка после исправления

После изменения `.env` запустите:

```bash
npx tsx scripts/test-db-connection.ts
```

Скрипт проверит все варианты подключения и покажет рабочий вариант.

## 📝 Текущие настройки

**DATABASE_URL:** использует pooler (порт 6543) - **НЕ РАБОТАЕТ**  
**Supabase URL:** `https://chkziqbxvdzwhlucfrza.supabase.co` - доступен  
**Проблема:** соединения сбрасываются (`ECONNRESET`)

## ⚠️ Важно

Если ничего не помогает:
1. Проверьте интернет соединение
2. Проверьте файрволл/антивирус (могут блокировать порты)
3. Попробуйте с другого компьютера/сети
4. Обратитесь в поддержку Supabase

## 📞 Контакты

- Supabase Support: https://supabase.com/support
- Supabase Discord: https://discord.supabase.com

