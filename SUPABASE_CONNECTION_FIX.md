# 🔧 Исправление проблем подключения к Supabase

## Проблема
База данных Supabase недоступна - соединения таймаутятся после 5-10 секунд ожидания.

## Причины
1. **Pooler недоступен** - порт 6543 может быть заблокирован
2. **Проблемы с SSL/TLS** - неправильные настройки SSL
3. **IP блокировка** - Supabase может блокировать IP адреса
4. **Лимиты соединений** - превышен лимит одновременных соединений

## Решения

### Вариант 1: Использовать прямое подключение (рекомендуется)

Измените `DATABASE_URL` в `.env`:

**Текущий (pooler - может быть заблокирован):**
```
DATABASE_URL=postgresql://postgres.chkziqbxvdzwhlucfrza:ПАРОЛЬ@aws-1-eu-west-2.pooler.supabase.com:6543/postgres
```

**Прямое подключение (более надежное):**
```
DATABASE_URL=postgresql://postgres.chkziqbxvdzwhlucfrza:ПАРОЛЬ@aws-1-eu-west-2.supabase.co:5432/postgres
```

**Или через транзакционный pooler (рекомендуется):**
```
DATABASE_URL=postgresql://postgres.chkziqbxvdzwhlucfrza:ПАРОЛЬ@aws-1-eu-west-2.pooler.supabase.com:6543/postgres?pgbouncer=true
```

### Вариант 2: Проверить настройки подключения

Убедитесь что в `server/repositories/index.ts` правильно настроен SSL:

```typescript
const client = postgres(connectionString, { 
  ssl: connectionString.includes('supabase') ? { 
    rejectUnauthorized: false 
  } : false,
  connect_timeout: 10,
  idle_timeout: 20,
  max_lifetime: 60 * 30,
  max: 10,
});
```

### Вариант 3: Проверить блокировку IP

1. Зайдите в Supabase Dashboard
2. Settings → Database → Connection Pooling
3. Проверьте "Allowed IP addresses"
4. Добавьте ваш текущий IP адрес

### Вариант 4: Использовать транзакционный режим pooler

Добавьте параметр `pgbouncer=true` к connection string:
```
DATABASE_URL=postgresql://...?pgbouncer=true&sslmode=require
```

## Быстрое исправление

1. Откройте `.env` файл
2. Найдите `DATABASE_URL`
3. Замените `pooler.supabase.com:6543` на `supabase.co:5432`
4. Или добавьте `?pgbouncer=true` в конец URL
5. Перезапустите сервер

## Проверка подключения

После изменения запустите:
```bash
npm run dev
```

И проверьте логи - не должно быть ошибок `ECONNRESET` или таймаутов.

