# Рекомендации по оптимизации проекта

## 📸 Оптимизация изображений

### Проблема
Изображения аватаров в `client/public/avatars/` имеют очень большой размер:
- Каждое изображение: **1.3-1.5 MB**
- Общий размер папки: **11 MB**
- Всего 8 изображений

### Рекомендации

1. **Конвертация в WebP формат**
   ```bash
   # Используйте инструмент для конвертации (например, sharp или imagemagick)
   # WebP может уменьшить размер на 25-35% без потери качества
   ```

2. **Оптимизация PNG**
   - Используйте инструменты: [ImageOptim](https://imageoptim.com/), [TinyPNG](https://tinypng.com/), или [Squoosh](https://squoosh.app/)
   - Ожидаемое уменьшение: до 70-80% от исходного размера

3. **Уменьшение разрешения**
   - Текущее разрешение вероятно слишком большое для аватаров
   - Рекомендуется: 200x200px или 400x400px для ретина-дисплеев
   - Максимальный размер файла после оптимизации: 50-100 KB

4. **Ленивая загрузка**
   - Используйте `<img loading="lazy">` для аватаров
   - Или используйте React компоненты с lazy loading

### Команды для оптимизации

```bash
# Используя sharp (Node.js)
npm install -D sharp
node scripts/optimize-images.js

# Или используя ImageMagick (если установлен)
find client/public/avatars -name "*.png" -exec convert {} -resize 400x400 -quality 85 {}.webp \;
```

## 🧪 Исправление тестов

### Выполнено
- ✅ Перемещены Playwright тесты из `tests/` в `tests/e2e/`
- ✅ Исправлен импорт в `tests/integration/helpers/fixtures.ts`

### Оставшиеся проблемы
- ⚠️ 20 тестов все еще падают (требуют дополнительной проверки)
- ⚠️ Некоторые тесты могут требовать настройки окружения

### Рекомендации
1. Запустить тесты после исправлений:
   ```bash
   npm run test:coverage
   ```

2. Проверить каждый упавший тест отдельно:
   ```bash
   npm run test tests/integration/api/companies.integration.test.ts
   ```

## 📦 Дополнительные оптимизации

### 1. Проверка размера bundle
```bash
npm run build
# Проверить размеры в dist/
```

### 2. Анализ зависимостей
```bash
npm run build -- --analyze
# Или использовать webpack-bundle-analyzer
```

### 3. Оптимизация импортов
- Убедитесь, что используются tree-shaking friendly импорты
- Например: `import { Button } from '@/ui/button'` вместо `import * from '@/ui'`

## 🎯 Приоритеты

1. **Высокий приоритет**
   - Оптимизация изображений (11 MB → ~1-2 MB)
   - Исправление упавших тестов

2. **Средний приоритет**
   - Анализ размера bundle
   - Проверка lazy loading компонентов

3. **Низкий приоритет**
   - Дополнительная оптимизация зависимостей
   - Аудит производительности

