# 🧪 Тестирование Yandex OAuth

## ✅ Что было исправлено:

1. **Добавлен fallback на recovery тип** - более надежный для OAuth пользователей
2. **Улучшено извлечение action_link** - проверяются разные структуры ответа
3. **Добавлен поиск URL в JSON** - на случай нестандартного формата
4. **Улучшено логирование** - больше деталей для отладки

## 📋 Шаги для тестирования:

### 1. Перезапустите сервер

```bash
# Остановите текущий процесс (Ctrl+C в терминале с сервером)
# Затем запустите заново:
npm run dev
```

### 2. Откройте браузер и перейдите на:

```
http://localhost:5173/login
```

(или `http://localhost:5174/login` если 5173 занят)

### 3. Нажмите "Войти через Яндекс"

### 4. Откройте терминал с логами сервера

Вы должны увидеть последовательность логов:
- `Yandex OAuth callback received`
- `Processing Yandex OAuth code exchange`
- `Token exchange successful`
- `Generating auth link for user`
- `generateLink response`
- `Extracted action link`
- `Redirecting to frontend`

### 5. Если видите ошибку:

Скопируйте **все строки с [ERROR]** из терминала и отправьте.

### 6. Проверьте переменные окружения

Убедитесь, что в `.env.local` есть:

```bash
SUPABASE_URL=...
SUPABASE_SERVICE_ROLE_KEY=...
YANDEX_CLIENT_ID=...
YANDEX_CLIENT_SECRET=...
```

---

## 🔍 Диагностика проблем:

### Проблема: "session_failed"

**Возможные причины:**
- Нет `SUPABASE_SERVICE_ROLE_KEY`
- Неправильный формат ответа от `generateLink`

**Решение:** Проверьте логи - там будет детальная информация

### Проблема: "server_error"

**Возможные причины:**
- Исключение на любом этапе процесса
- Проблема с базой данных
- Проблема с Supabase API

**Решение:** Скопируйте полный стек ошибки из логов

---

## 💡 Важно:

После изменений в коде **обязательно перезапустите сервер**, иначе изменения не применятся!

