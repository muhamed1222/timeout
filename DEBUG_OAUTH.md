# 🔍 Инструкция по отладке OAuth ошибки "server_error"

## ❗ Проблема

В консоли браузера видна ошибка:
```
error: "server_error"
errorDescription: null
```

Это означает, что произошло исключение в catch блоке сервера.

## 📋 Что делать:

### Шаг 1: Найдите терминал, где запущен сервер

Откройте терминал, где вы запустили `npm run dev`. Там должны быть логи сервера.

### Шаг 2: Попробуйте войти через Яндекс

Нажмите кнопку "Войти через Яндекс" на странице `/login`.

### Шаг 3: Сразу смотрите логи сервера

В терминале должны появиться логи. **ВНИМАТЕЛЬНО** смотрите на строки с `[ERROR]`.

### Шаг 4: Найдите ПЕРВУЮ ошибку

Ошибки могут появляться в такой последовательности:

#### ✅ Нормальный поток (до ошибки):

```
[INFO] Yandex OAuth callback received { query: {...}, cookies: {...} }
[INFO] Processing Yandex OAuth code exchange { code: '...' }
[INFO] Exchanging code for token { ... }
[INFO] Token exchange successful
[INFO] Got access token from Yandex
[INFO] Fetching user info from Yandex
[INFO] Checking if user exists { email: '...' }
```

#### ❌ После этого может быть ошибка:

Смотрите на строки с `[ERROR]`, например:

```
[ERROR] Error in Yandex OAuth callback {
  error: '...',
  stack: '...',
  name: '...',
  code: '...'
}
```

ИЛИ

```
[ERROR] Failed to create company { error: {...} }
```

ИЛИ

```
[ERROR] Failed to generate auth link { error: {...} }
```

ИЛИ

```
[ERROR] Exception in generateLink {
  error: '...',
  stack: '...'
}
```

### Шаг 5: Скопируйте ВСЁ

**Скопируйте ВСЕ логи** из терминала, начиная с момента нажатия кнопки "Войти через Яндекс".

Особенно важно:
- Все строки с `[ERROR]`
- Все строки с `[INFO]`, которые идут перед первой ошибкой
- Стеки ошибок (строки, начинающиеся с "at ")

---

## 🎯 Типичные ошибки и решения:

### Ошибка 1: "Failed to create company"

**Логи:**
```
[ERROR] Failed to create company { error: {...} }
```

**Возможные причины:**
- Нет подключения к базе данных
- Неправильные переменные окружения `DATABASE_URL`
- Проблемы с правами доступа к БД

**Решение:** Проверьте `DATABASE_URL` в `.env.local`

---

### Ошибка 2: "Failed to generate auth link"

**Логи:**
```
[ERROR] Failed to generate auth link { 
  errorMessage: '...',
  errorStatus: ...,
  ...
}
```

**Возможные причины:**
- Нет `SUPABASE_SERVICE_ROLE_KEY`
- Неправильный `SUPABASE_URL`
- Пользователь уже существует, но с другой конфигурацией

**Решение:** 
1. Проверьте `SUPABASE_SERVICE_ROLE_KEY` в `.env.local`
2. Проверьте `SUPABASE_URL` в `.env.local`

---

### Ошибка 3: "Exception in generateLink"

**Логи:**
```
[ERROR] Exception in generateLink {
  error: '...',
  stack: '...'
}
```

**Возможные причины:**
- Проблема с форматом данных от Supabase
- Версия `@supabase/supabase-js` несовместима
- Проблема с сетью при обращении к Supabase

**Решение:**
1. Проверьте версию: `npm list @supabase/supabase-js`
2. Попробуйте обновить: `npm update @supabase/supabase-js`
3. Проверьте доступность Supabase URL

---

### Ошибка 4: "Error getting user by email"

**Логи:**
```
[ERROR] Error getting user by email { error: {...}, email: '...' }
```

**Возможные причины:**
- Проблема с Supabase Admin API
- Неправильный `SUPABASE_SERVICE_ROLE_KEY`

**Решение:** Проверьте `SUPABASE_SERVICE_ROLE_KEY`

---

## 📤 Что отправить для помощи:

Когда скопируете логи, отправьте:

1. **Все строки с `[ERROR]`** (полностью, включая стек)
2. **Последние 10-15 строк с `[INFO]` перед первой ошибкой**
3. **Список переменных окружения** (без значений, только названия):
   ```bash
   cat .env.local | grep -E "(SUPABASE|DATABASE|YANDEX)" | cut -d'=' -f1
   ```

---

## 💡 Совет:

Если логи не появляются вообще:
1. Убедитесь, что сервер действительно запущен
2. Проверьте, что вы смотрите в правильный терминал
3. Попробуйте перезапустить сервер: `npm run dev`

