# Security settings
audit=true
audit-level=moderate
fund=false

# Use exact versions for security patches
save-exact=false

# Ignore optional dependencies warnings during install
legacy-peer-deps=false

