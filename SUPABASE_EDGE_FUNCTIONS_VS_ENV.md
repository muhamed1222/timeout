# ⚠️ Edge Function Secrets vs .env.local

## Важное уточнение

**Edge Function Secrets** в Supabase Dashboard — это секреты для **Supabase Edge Functions** (серверлесс функций).

**Наше приложение** использует **собственный Express.js сервер**, который запускается локально на порту 3001 и читает переменные из `.env.local`.

---

## 🔍 Разница

### Edge Function Secrets (Supabase Dashboard)
- Используются **только** в Supabase Edge Functions
- Это серверлесс функции, которые запускаются на серверах Supabase
- **НЕ используются** нашим локальным Node.js/Express сервером
- Нужны только при деплое на Supabase Edge Functions

### .env.local (локальный файл)
- Используется **нашим локальным сервером** (Express.js на порту 3001)
- Читается при запуске `npm run dev`
- **Это то, что нужно для локальной разработки**

---

## ✅ Что нужно для работы OAuth локально

Переменные **уже есть** в вашем `.env.local`:

```env
YANDEX_CLIENT_ID=2710959716ba49f48959f7a533e7aadd
YANDEX_CLIENT_SECRET=6d9c6fa7a40042a7bebadc3edeb5df77
```

**Это правильно и этого достаточно!** 🎉

---

## 🚀 Что делать дальше

Edge Function Secrets можно оставить (они не помешают), но для локальной разработки они **не используются**.

### Главное — перезапустите сервер:

```bash
# В терминале с npm run dev:
# 1. Остановите (Ctrl+C)
# 2. Запустите заново:
npm run dev
```

Сервер прочитает переменные из `.env.local` при запуске.

---

## 📝 Когда нужны Edge Function Secrets?

Edge Function Secrets понадобятся, если вы:
1. Решите деплоить приложение на Supabase Platform
2. Будете использовать Supabase Edge Functions вместо Express.js
3. Перепишете OAuth на Edge Functions

**Для локальной разработки с Express.js** они **не нужны**.

---

## ✅ Проверка

После перезапуска сервера попробуйте войти через Яндекс:

1. Откройте `http://localhost:5173/login`
2. Откройте DevTools (F12) → Console
3. Нажмите "Войти через Яндекс"
4. Проверьте логи в консоли браузера и терминале

---

## 🎯 Итог

- ✅ `.env.local` с Yandex переменными — **есть и правильно настроен**
- ✅ Supabase переменные — **есть и правильно настроены**
- ⚠️ Edge Function Secrets — **не нужны для локальной разработки**
- 🔄 Осталось только **перезапустить сервер** и попробовать войти

Попробуйте войти через Яндекс после перезапуска сервера! 🚀

